import { FormEvent, useState } from 'react'; import { Link, useNavigate } from 'react-router-dom'; import { api } from '../lib/api'; import { useAuth } from '../store/auth.store'; import { useCart } from '../store/cart.store';
export default function Login() { const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [error, setError] = useState(''); const navigate = useNavigate(); const submit = async (e: FormEvent) => { e.preventDefault(); try { const { data } = await api.post('/auth/login', { email, password }); useAuth.getState().setTokens(data.accessToken, data.refreshToken); useAuth.getState().setUser(data.user); await useCart.getState().syncOnLogin(); navigate('/'); } catch (e: any) { setError(e.response?.data?.message || 'Đăng nhập thất bại'); } }; return <form className="max-w-md mx-auto py-16 space-y-4" onSubmit={submit}><h1>Đăng nhập</h1>{error && <p>{error}</p>}<input className="border p-2 w-full" type="email" value={email} onChange={e => setEmail(e.target.value)} placeholder="Email" /><input className="border p-2 w-full" type="password" value={password} onChange={e => setPassword(e.target.value)} placeholder="Mật khẩu" /><button type="submit">Đăng nhập</button><Link to="/register">Đăng ký</Link></form>; }