import { useEffect, useState } from 'react'; import { useParams } from 'react-router-dom'; import { api } from '../lib/api'; import { useCart } from '../store/cart.store';
export default function ProductDetail() { const { id } = useParams(); const [product, setProduct] = useState<any>(); const add = useCart(s => s.addItem); useEffect(() => { api.get(`/products/${id}`).then(({ data }) => setProduct(data)).catch(() => setProduct(null)); }, [id]); if (!product) return <p className="py-10">Đang tải sản phẩm...</p>; const v = product.variants?.[0]; return <section className="py-10"><h1 className="text-4xl">{product.name}</h1><p>{product.description}</p>{v && <button onClick={() => add({ variant: v.id, name: product.name, price: v.price, quantity: 1, image: product.images?.[0], volume: v.volume, stock: v.stock })}>Thêm vào giỏ</button>}</section>; }