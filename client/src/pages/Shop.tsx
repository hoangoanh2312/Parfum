import { useEffect, useState } from 'react'; import { api } from '../lib/api'; import ProductCard, { ProductCardData } from '../components/ProductCard';
export default function Shop() { const [products, setProducts] = useState<ProductCardData[]>([]); const [query, setQuery] = useState(''); useEffect(() => { api.get('/products', { params: { limit: 100 } }).then(({ data }) => setProducts(Array.isArray(data) ? data : data.data || [])).catch(() => undefined); }, []); const list = products.filter(p => p.name.toLowerCase().includes(query.toLowerCase())); return <section className="py-10"><h1 className="text-4xl">Sản phẩm</h1><input className="border p-2 mt-6" value={query} onChange={e => setQuery(e.target.value)} placeholder="Tìm sản phẩm" /><div className="grid md:grid-cols-4 gap-6 mt-8">{list.map(p => <ProductCard key={p.id} item={p} />)}</div></section>; }