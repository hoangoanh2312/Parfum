import { Link } from 'react-router-dom'; import { useCart } from '../store/cart.store';
export interface ProductCardData { id: string; slug?: string; name: string; brand?: string; image?: string | null; price?: number | null; priceText?: string; variantId?: string | null; volume?: string; stock?: number; gender?: string }
export default function ProductCard({ item }: { item: ProductCardData }) { const add = useCart(s => s.addItem); return <article className="bg-white p-4 border"><Link to={`/products/${item.slug || item.id}`}><img className="w-full h-56 object-cover" src={item.image || 'https://placehold.co/400x500'} alt={item.name} /><p>{item.brand}</p><h3>{item.name}</h3></Link><p>{item.priceText || (item.price || 0).toLocaleString('vi-VN') + 'đ'}</p><button disabled={!item.variantId || item.stock === 0} onClick={() => item.variantId && add({ variant: item.variantId, name: item.name, price: item.price || 0, quantity: 1, image: item.image, volume: item.volume, stock: item.stock })}>Thêm vào giỏ</button></article>; }
