import { Link } from 'react-router-dom'; import { useCart } from '../store/cart.store'; import { useAuth } from '../store/auth.store';
export default function Header() { const count = useCart(s => s.count); const user = useAuth(s => s.user); return <header className="p-5 border-b flex gap-5"><Link to="/">HOC PARFUM</Link><Link to="/shop">Sản phẩm</Link><Link to="/cart">Giỏ ({count})</Link><Link to={user ? '/account' : '/login'}>{user?.name || 'Đăng nhập'}</Link></header>; }
